#ifndef TRAINING_TYPES_H
#define TRAINING_TYPES_H

// Managers
typedef struct s_game t_game;
typedef struct s_spaceMarineArmy t_spaceMarineArmy;
typedef struct s_zergSwarm t_zergSwarm;

// Entities
typedef struct s_entity t_entity;
typedef struct s_spaceMarine t_spaceMarine;
typedef struct s_zerg t_zerg;

// Items
typedef struct s_inventory t_inventory;
typedef struct s_weapon t_weapon;
typedef struct s_armor t_armor;

#endif //TRAINING_TYPES_H
